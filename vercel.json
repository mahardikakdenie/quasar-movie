{
  "builds": [
    {
      "src": "src/index.html",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/spa"
      }
    }
  ],
  "routes": [{ "handle": "filesystem" }, { "src": "/.*", "dest": "/" }]
}
